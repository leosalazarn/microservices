# Infrastructure credentials — local development only
# These values are the defaults used by docker-compose.yml.
# In production, use Vault or a proper secret manager.

VAULT_TOKEN=myroot
MONGO_PASSWORD=password
REDIS_PASSWORD=redispassword
